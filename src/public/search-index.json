[
  {
    "title": "AboutPage",
    "path": "/AboutPage.jsx",
    "text": "{ if (window.innerWidth window.removeEventListener(\"resize\", handleResize); }, []); return ( Welcome To Marche Healthcare {/* */} Our story ( {char} ( {char} ( {char} ( {char} ( {char} ( {char} ( {char} ( {char} ( {char} ( {char} ( {char} ( {char} ( {char} ))} Welcome to Marche Health Care, where innovation meets dedication. Founded in 2020, we are a forward-thinking startup committed to revolutionizing the biomedical field. {/* Our primary focus is on the innovation and development of cutting-edge biomedical instruments designed to empower healthcare professionals. */} {/* */} {/* At Marche Health Care, we believe in pushing the boundaries of medical technology to enhance the capabilities of those on the frontlines of healthcare. Our team of experts is dedicated to creating solutions that improve patient outcomes and streamline medical processes. Join us on our journey to transform healthcare through innovation and excellence. */} {mobileView ? ( Our Purpose \"Marche Healthcare is dedicated to addressing health disparities by developing innovative medical devices that ensure equal access to high-quality care for all\" Dr. Mari Raj Founder & CEO {\" \"} ) : ( Our Purpose \"Marche Healthcare is dedicated to addressing health disparities by developing innovative medical devices that ensure equal access to high-quality care for all\" Dr. Mari Raj Founder & CEO {\" \"} )} Uniting Talented Minds Together, we create transformative solutions that promote health equality Empower Healthcare Professionals Our innovative solutions enhance their capabilities and streamline workflows Advancing Health Equality Ensure that everyone, regardless of their background, receives the highest standard of healthcare Our Vision {\" \"} Our vision is a world where health equality is not just an aspiration but a fundamental human right, where every individual, regardless of background or circumstance, has access to equitable healthcare and opportunities for a healthy life. Our Team With a shared passion for improving global health outcomes, our team is dedicated to making a meaningful impact in the healthcare industry Board of Directors Scientific Advisors { return ( ); }) ) : ( )} {selctedCardList[0].isActive ? ( ) : ( )} react react-router-dom framer-motion ../Layouts/Layouts ./css/aboutpage.css ../assets/about/aboutbanner.png ../components/CardUserProfile ../components/UserFullDetails ../components/ScrollingContainer ../utils/SplitStringUsingRegex Our vision is a world where health equality is not just an aspiration but a fundamental human right, where every individual, regardless of background or circumstance, has access to equitable healthcare and opportunities for a healthy life. Innovation is at the heart of everything we do. We believe that groundbreaking ideas and state-of-the- art technology have the power to transform healthcare and promote health equality. ; const heroText = SplitStringUsingRegex(HeroText); const heroText2 = SplitStringUsingRegex(HeroText2); const heroText3 = SplitStringUsingRegex(HeroText3); const heroText4 = SplitStringUsingRegex(HeroText4); const heroText5 = SplitStringUsingRegex(HeroText5); const heroText6 = SplitStringUsingRegex(HeroText6); const blockText = SplitStringUsingRegex(BlockText); const blockText2 = SplitStringUsingRegex(BlockText2); const blockText3 = SplitStringUsingRegex(BlockText3); const blockText4 = SplitStringUsingRegex(BlockText4); const blockText5 = SplitStringUsingRegex(BlockText5); const blockText6 = SplitStringUsingRegex(BlockText6); const blockText7 = SplitStringUsingRegex(BlockText7); const USERS = { boardofdirectors: [ { id: 1, isActive: false, profileImg: , name: , position: , twitter: , linkedin: , instagram: , }, { id: 2, isActive: false, // profileImg: , profileImg: , name: , position: , twitter: , linkedin: , instagram: , }, ], scientificadvisors: [ { id: 1, isActive: false, profileImg: , name: , position: , twitter: , linkedin: , instagram: , }, { id: 2, isActive: false, profileImg: , name: , position: , twitter: , linkedin: , instagram: , }, { id: 3, isActive: false, profileImg: , name: , position: , twitter: , linkedin: , instagram: , }, { id: 4, isActive: false, profileImg: , name: , position: , twitter: , linkedin: , instagram: , }, ], }; export default function AboutPage() { const [selectdTab, setSelectdTab] = useState( ); const [selctedCardList, setSelctedCardList] = useState(USERS[selectdTab]); function handleSelectUserBlog(username) { setSelectdTab(username); setSelctedCardList(USERS[username.toString()]); } // navigaiton const location = useLocation(); useEffect(() => { if (location.hash) { const element = document.getElementById(location.hash.slice(1)); if (element) { element.scrollIntoView({ behavior: }); } } }, [location]); const [mobileView, setMobileView] = useState(false); useEffect(() => { const handleResize = () => { if (window.innerWidth   window.removeEventListener( , handleResize); }, []); return (        Welcome   To   Marche Healthcare            {/*   */}    Our story    {blockText.map((char) => (   {char}   ))} {heroText.map((char) => (   {char}   ))} {blockText3.map((char) => (   {char}   ))} {heroText2.map((char) => (   {char}   ))} {blockText4.map((char) => (   {char}   ))} {heroText3.map((char) => (   {char}   ))} {blockText5.map((char) => (   {char}   ))} {heroText4.map((char) => (   {char}   ))} {blockText6.map((char) => (   {char}   ))} {heroText5.map((char) => (   {char}   ))} {blockText7.map((char) => (   {char}   ))} {heroText6.map((char) => (   {char}   ))} {blockText2.map((char) => (   {char}   ))}       Welcome to Marche Health Care, where innovation meets dedication. Founded in 2020, we are a forward-thinking startup committed to revolutionizing the biomedical field. {/* Our primary focus is on the innovation and development of cutting-edge biomedical instruments designed to empower healthcare professionals. */} {/*     */} {/* At Marche Health Care, we believe in pushing the boundaries of medical technology to enhance the capabilities of those on the frontlines of healthcare. Our team of experts is dedicated to creating solutions that improve patient outcomes and streamline medical processes. Join us on our journey to transform healthcare through innovation and excellence. */}                 {mobileView ? (    Our Purpose               Dr. Mari Raj   Founder & CEO               { }                 ) : (    Our Purpose               Dr. Mari Raj   Founder & CEO               { }                 )}                  Our Team    With a shared passion for improving global health outcomes, our team is dedicated to making a meaningful impact in the healthcare industry           { handleSelectUserBlog( ); }} > Board of Directors     { handleSelectUserBlog( ); }} > Scientific Advisors       {selctedCardList ? ( selctedCardList.map((userdetails, idx) => { return (   { setSelctedCardList((prevList) => { return [ { ...prevList[0], ...selctedCardList.filter( (value) => value.id === userdetails.id )[0], isActive: , }, ]; }); }} /> ); }) ) : ( <>  )}   {selctedCardList[0].isActive ? (   { const removeValue = selctedCardList[0].id; return obj["
  },
  {
    "title": "AdminLayout",
    "path": "/admin/AdminLayout.jsx",
    "text": "{label} window.removeEventListener(\"keydown\", onKeyDown); }, []); return ( ◄ Marche • Admin ↵ {/* Quick Create */} ⊕ Quick Create {openQC && ( New Video New Career New Broadcast New Testimonial )} {/* User Menu */} Admin ⌄ {openUser && ( Profile Audit Logs Sign out )} react react-router-dom ../../auth/AuthContext ./admin-css/admin.css admin-nav-item ${active ? : }> {label}   ); } export default function AdminLayout() { const navigate = useNavigate(); // Search state/refs const [q, setQ] = useState( ); const searchRef = useRef(null); const { logout } = useAuth(); // Menus state/refs const [openQC, setOpenQC] = useState(false); const [openUser, setOpenUser] = useState(false); const qcRef = useRef(null); const userRef = useRef(null); // Submit search function submitSearch(e) { e.preventDefault(); const query = (q || ).trim(); if (!query) return; navigate( ); } // Close menus on outside click useEffect(() => { function onDocMouseDown(ev) { if (openQC && qcRef.current && !qcRef.current.contains(ev.target)) { setOpenQC(false); } if (openUser && userRef.current && !userRef.current.contains(ev.target)) { setOpenUser(false); } } document.addEventListener( , onDocMouseDown); return () => document.removeEventListener( , onDocMouseDown); }, [openQC, openUser]); // Keyboard shortcut: Ctrl/Cmd + K focuses search useEffect(() => { function onKeyDown(e) { if ((e.ctrlKey || e.metaKey) && (e.key === || e.key === )) { e.preventDefault(); searchRef.current?.focus(); } } window.addEventListener( , onKeyDown); return () => window.removeEventListener( , onKeyDown); }, []); return (        ◄ Marche • Admin        setQ(e.target.value)} className= placeholder= aria-label= />  ↵      {/* Quick Create */}     setOpenQC((v) => !v)} aria-haspopup= aria-expanded={openQC} > ⊕ Quick Create   {openQC && (     { setOpenQC(false); navigate( ); }} > New Video     { setOpenQC(false); navigate( ); }} > New Career     { setOpenQC(false); navigate( ); }} > New Broadcast     { setOpenQC(false); navigate( ); }} > New Testimonial     )}   {/* User Menu */}     setOpenUser((v) => !v)} aria-haspopup= aria-expanded={openUser} > Admin ⌄   {openUser && (     { setOpenUser(false); navigate( ); }} > Profile     { setOpenUser(false); navigate( ); }} > Audit Logs     { await logout(); navigate( , { replace: true }); }} > Sign out     )}                          "
  },
  {
    "title": "AdminLogin",
    "path": "/admin/AdminLogin.jsx",
    "text": "({})); if (!res.ok || !data?.token) { const msg = data?.error || `HTTP ${res.status}`; throw new Error( msg === \"Invalid email or password\" ? msg : \"Login failed\" ); } // persist auth (token, profile) via your AuthContext login({ token: data.token, admin: data.admin || { email: form.email } }); nav(from, { replace: true }); } catch (err) { setError(err?.message || \"Login failed\"); } finally { setSubmitting(false); } } return ( Admin Login Welcome back. Please sign in to continue. {error && ( {error} )} Email Password {showPwd ? \"Hide\" : \"Show\"} {submitting ? \"Signing in…\" : \"Sign In\"} ← Back to site react react-router-dom ../../auth/AuthContext ./admin-css/admin-login.css http://localhost:8080/api/admin /admin , password: }); const [showPwd, setShowPwd] = useState(false); const [error, setError] = useState( ); // 🔧 Local submit state instead of context.setLoading const [submitting, setSubmitting] = useState(false); function onChange(e) { const { name, value } = e.target; setForm((s) => ({ ...s, [name]: value })); } async function onSubmit(e) { e.preventDefault(); setError( ); if (!form.email || !form.password) { setError( ); return; } try { setSubmitting(true); const res = await fetch( , { method: , headers: { application/json include HTTP ${res.status} Invalid email or password Login failed Login failed auth-page auth-card auth-brand /companyLogo.png Marche Healthcare auth-error alert auth-form auth-label auth-input email email you@company.com email auth-label auth-input-group auth-input text password password •••••••• current-password button ghost-btn Hide Show auth-submit Signing in… Sign In auth-footer className= > ← Back to site              "
  },
  {
    "title": "BroadcastsAdmin",
    "path": "/admin/BroadcastsAdmin.jsx",
    "text": "r.id !== id)); } catch (e) { console.error(e); alert(e.message || \"Delete failed\"); } }; return ( + New Broadcast Social Media News & Events ID Title Category Status Schedule Actions {loading ? ( Loading… ) : rows.length === 0 ? ( No broadcasts yet. ( {r.id} {r.title} { (r.kind ?? r.category) === 'social' && r.social_source && ( {r.social_source} )} {r.kind ?? r.category} {r.status} {r.publish_at ? new Date(r.publish_at).toLocaleString() : \"—\"} Preview Edit Delete ))} {showForm && ( )} {preview && ( )} react ./components/PreviewModal ./components/BroadcastFormModal ../../components/SectionHeader ../../auth/AuthContext ./admin-css/admin.css /api/admin social kind ); const [rows, setRows] = useState([]); const [loading, setLoading] = useState(false); const [showForm, setShowForm] = useState(false); const [editing, setEditing] = useState(null); const [preview, setPreview] = useState(null); async function authFetch(url, init = {}) { const hdrs = { ...(init.headers || {}), Authorization: }; // Only set JSON header when NOT sending FormData if (!(init.body instanceof FormData)) hdrs[ ] = ; const res = await fetch(url, { ...init, headers: hdrs }); const text = await res.text(); let data = null; try { data = text ? JSON.parse(text) : null; } catch {} if (!res.ok) throw new Error((data && data.error) || ); return { data, status: res.status }; } async function load() { setLoading(true); try { const url = new URL( , window.location.origin); url.searchParams.set( , tab); // backend expects ?kind= if (query) url.searchParams.set( , query); const { data } = await authFetch(url.toString()); // backend returns an array of rows setRows(Array.isArray(data) ? data : (data?.items || data?.rows || [])); } catch (e) { console.error(e); setRows([]); } finally { setLoading(false); } } useEffect(() => { load(); /* eslint-disable-next-line */ }, [tab, query]); const openNew = () => { setEditing(null); setShowForm(true); }; const openEdit = async (id) => { try { const { data } = await authFetch( ); // your authFetch returns {data} const row = data?.item ?? data; // unwrap if server responds { ok, item } if (!row?.id) throw new Error( ); setEditing(row); setShowForm(true); } catch (e) { console.error(e); alert(e.message || ); } }; const doDelete = async (id) => { if (!confirm( )) return; try { const { status } = await authFetch( , { method: }); if (status === 204) setRows((rows) => rows.filter((r) => r.id !== id)); } catch (e) { console.error(e); alert(e.message || ); } }; return (          + New Broadcast         setTab( )}>Social Media   setTab( )}>News & Events       setQuery(e.target.value)} />        ID  Title  Category  Status  Schedule  Actions    {loading ? (  Loading…  ) : rows.length === 0 ? (  No broadcasts yet.  ) : rows.map(r => (    {r.id}    {r.title} { (r.kind ?? r.category) === && r.social_source && (  {r.social_source}  )}    {r.kind ?? r.category}   {r.status}   {r.publish_at ? new Date(r.publish_at).toLocaleString() : }     setPreview(r)}>Preview   openEdit(r.id)}>Edit   doDelete(r.id)}>Delete      ))}   {showForm && (  "
  },
  {
    "title": "CareersAdmin",
    "path": "/admin/CareersAdmin.jsx",
    "text": "{ setEditId(id); setShowForm(true); }; async function onDelete(id) { if (!confirm(\"Delete this job?\")) return; try { await authFetch(`${API_BASE}/careers/${id}`, { method: \"DELETE\" }); load(); } catch (e) { alert(e.message || \"Delete failed\"); } } return ( + Add Job Full-time Internship ID Title Category Status Updated Actions {loading ? ( Loading… ) : rows.length === 0 ? ( No jobs found. ( {r.id} {r.title} {r.category} {r.status} {new Date(r.updated_at).toLocaleString()} Edit Delete ))} {showForm && ( )} react ../../components/SectionHeader ./components/CareerFormModal ../../auth/AuthContext ./admin-css/admin.css /api/admin fulltime ); const [rows, setRows] = useState([]); const [loading, setLoading] = useState(false); const [showForm, setShowForm] = useState(false); const [editId, setEditId] = useState(null); async function authFetch(url, init = {}) { const headers = { ...(init.headers || {}), Authorization: }; if (!(init.body instanceof FormData)) headers[ ] = ; const res = await fetch(url, { ...init, headers }); // ✅ no credentials, Bearer only const text = await res.text(); let data = null; try { data = text ? JSON.parse(text) : null; } catch {} if (!res.ok) throw new Error((data && data.error) || ); return { data, status: res.status }; } async function load() { setLoading(true); try { const url = new URL( , window.location.origin); url.searchParams.set( , category); if (query) url.searchParams.set( , query); const { data } = await authFetch(url.toString()); setRows(data?.items || data || []); } catch (e) { console.error(e); setRows([]); } finally { setLoading(false); } } useEffect(() => { load(); /* eslint-disable-next-line */ }, [category, query]); const openAdd = () => { setEditId(null); setShowForm(true); }; const openEdit = (id) => { setEditId(id); setShowForm(true); }; async function onDelete(id) { if (!confirm( )) return; try { await authFetch( , { method: }); load(); } catch (e) { alert(e.message || ); } } return (          + Add Job         setCategory( )}>Full-time   setCategory( )}>Internship       setQuery(e.target.value)} />        ID  Title  Category  Status  Updated  Actions    {loading ? (  Loading…  ) : rows.length === 0 ? (  No jobs found.  ) : rows.map(r => (    {r.id}   {r.title}   {r.category}   {r.status}   {new Date(r.updated_at).toLocaleString()}     openEdit(r.id)}>Edit   "
  },
  {
    "title": "BroadcastFormModal",
    "path": "/admin/components/BroadcastFormModal.jsx",
    "text": "({ ...s, [name]: value })); } async function authFetch(url, init = {}) { const headers = { ...(init.headers || {}), Authorization: `Bearer ${token}` }; // Do not set Content-Type when sending FormData const res = await fetch(url, { ...init, headers }); const text = await res.text(); let data = null; try { data = text ? JSON.parse(text) : null; } catch {} if (!res.ok) throw new Error((data && data.error) || `HTTP ${res.status}`); return { data, status: res.status }; } async function onSubmit(e) { e.preventDefault(); setErr(\"\"); if (!form.title || !validKinds.includes(form.kind)) { setErr(\"Please provide a title and valid category.\"); return; } try { setSaving(true); // Build multipart body const fd = new FormData(); fd.append(\"kind\", form.kind); fd.append(\"title\", form.title); if (form.summary) fd.append(\"summary\", form.summary); if (form.body) fd.append(\"body\", form.body); if (form.status && validStatuses.includes(form.status)) fd.append(\"status\", form.status); if (form.publish_at) { // convert local datetime-local to ISO fd.append(\"publish_at\", new Date(form.publish_at).toISOString()); } if (form.kind === \"social\" && form.social_source) fd.append(\"social_source\", form.social_source); if (imageFile) fd.append(\"image\", imageFile); // This is the missing line 🎯 if (form.link_url) fd.append(\"link_url\", form.link_url); if (form.id) { await authFetch(`${apiBase}/broadcasts/${form.id}`, { method: \"PUT\", body: fd }); } else { // Image is required on create—enforce in UI if (!imageFile) { setErr(\"Please upload an image.\"); setSaving(false); return; } await authFetch(`${apiBase}/broadcasts`, { method: \"POST\", body: fd }); } onSaved(); } catch (e) { setErr(e.message || \"Save failed\"); } finally { setSaving(false); } } return ( {form.id ? \"Edit Broadcast\" : \"New Broadcast\"} × {err && {err} } Category social news {form.kind === \"social\" ? ( Source (select) LinkedIn Twitter / X Instagram YouTube ) : } Link URL Title Summary Body (HTML allowed) Image Upload {form.id ? \"(optional on edit)\" : \"(required)\"} Status {s} )} Publish At (optional) Cancel {saving ? \"Saving…\" : \"Save\"} react ../admin-css/modals/base.css ../admin-css/modals/broadcast.css social news draft published archived ; const d = new Date(iso); const pad = (n) => String(n).padStart(2, ); return ; } export default function BroadcastFormModal({ apiBase, token, initial, onClose, onSaved }) { const [form, setForm] = useState({ id: initial?.id || null, kind: initial?.kind || initial?.category || , title: initial?.title || , summary: initial?.summary || , body: initial?.body ?? initial?.body_html ?? , status: initial?.status || , publish_at: toLocalInputValue(initial?.publish_at ?? initial?.scheduled_at), social_source: initial?.social_source || , // new: preserve existing link_url if present link_url: initial?.link_url || , image_path: initial?.image_path || initial?.image_url || , }); const [imageFile, setImageFile] = useState(null); const [saving, setSaving] = useState(false); const [err, setErr] = useState( ); function onChange(e) { const { name, value } = e.target; setForm(s => ({ ...s, [name]: value })); } async function authFetch(url, init = {}) { const headers = { ...(init.headers || {}), Authorization: }; // Do not set Content-Type when sending FormData const res = await fetch(url, { ...init, headers }); const text = await res.text(); let data = null; try { data = text ? JSON.parse(text) : null; } catch {} if (!res.ok) throw new Error((data && data.error) || ); return { data, status: res.status }; } async function onSubmit(e) { e.preventDefault(); setErr( ); if (!form.title || !validKinds.includes(form.kind)) { setErr( ); return; } try { setSaving(true); // Build multipart body const fd = new FormData(); fd.append( , form.kind); fd.append( , form.title); if (form.summary) fd.append( , form.summary); if (form.body) fd.append( , form.body); if (form.status && validStatuses.includes(form.status)) fd.append( , form.status); if (form.publish_at) { // convert local datetime-local to ISO fd.append( , new Date(form.publish_at).toISOString()); } if (form.kind === && form.social_source) fd.append( , form.social_source); if (imageFile) fd.append( , imageFile); // This is the missing line 🎯 if (form.link_url) fd.append( , form.link_url); if (form.id) { await authFetch( , { method: , body: fd }); } else { // Image is required on create—enforce in UI if (!imageFile) { setErr( ); setSaving(false); return; } await authFetch( , { method: , body: fd }); } onSaved(); } catch (e) { setErr(e.message || ); } finally { setSaving(false); } } return (    e.stopPropagation()}>    {form.id ? : }   ×      {err &&  {err} }    Category    social   news      {form.kind === ? (  Source    (select)   LinkedIn   Twitter / X   Instagram   YouTube      ) :  }  Link URL      Title      Summary      Body (HTML allowed)      Image Upload {form.id ? : }  setImageFile(e.target.files?.[0] || null)} />    Status   {validStatuses.map(s =>  {s} )}      Publish At (optional)        Cancel   {saving ? :"
  },
  {
    "title": "CareerFormModal",
    "path": "/admin/components/CareerFormModal.jsx",
    "text": "{ try { const { data } = await authFetch(`${API_BASE}/careers/${editId}`); const it = data?.item || data; if (it) { setForm({ title: it.title ?? \"\", category: it.category ?? \"fulltime\", status: it.status ?? \"draft\", location: it.location ?? \"\", experience: it.experience ?? \"\", salary: it.salary ?? \"\", skills: it.skills ?? \"\", apply_url: it.apply_url ?? \"\", apply_email: it.apply_email ?? \"\", description: it.description ?? \"\", }); } } catch (e) { setError(e.message || \"Failed to load\"); } })(); } else if (open) { setForm({ title: \"\", category: \"fulltime\", status: \"draft\", location: \"\", experience: \"\", salary: \"\", skills: \"\", apply_url: \"\", apply_email: \"\", description: \"\", }); } }, [open, editId, API_BASE]); // eslint-disable-line async function save() { setSaving(true); setError(\"\"); try { if (!form.title.trim()) { setError(\"Title is required\"); return; } const url = editId ? `${API_BASE}/careers/${editId}` : `${API_BASE}/careers`; const method = editId ? \"PUT\" : \"POST\"; const { data } = await authFetch(url, { method, body: JSON.stringify(form) }); if (data?.error) { setError(data.error); return; } onSaved?.(); onClose?.(); } catch (e) { setError(e.message || \"Failed to save\"); } finally { setSaving(false); } } if (!open) return null; return ( {editId ? \"Edit Job\" : \"Add Job\"} ✕ Title Category Full-time Internship Status Draft Published Archived Location Experience Salary Skills (comma separated) Apply URL Apply Email Description {error && {error} } Cancel {saving ? \"Saving…\" : \"Save\"} react ../admin-css/modals/base.css ../admin-css/modals/career.css , category: , // ✅ only fulltime|internship status: , location: , experience: , salary: , skills: , apply_url: , apply_email: , description: , }); const [saving, setSaving] = useState(false); const [error, setError] = useState( ); async function authFetch(url, init = {}) { const headers = { ...(init.headers || {}), Authorization: }; if (!(init.body instanceof FormData)) headers[ ] = ; const res = await fetch(url, { ...init, headers }); // ✅ no credentials here const text = await res.text(); let data = null; try { data = text ? JSON.parse(text) : null; } catch {} if (!res.ok) throw new Error((data && data.error) || ); return { data, status: res.status }; } useEffect(() => { setError( ); if (open && editId) { (async () => { try { const { data } = await authFetch( ); const it = data?.item || data; if (it) { setForm({ title: it.title ?? , category: it.category ?? , status: it.status ?? , location: it.location ?? , experience: it.experience ?? , salary: it.salary ?? , skills: it.skills ?? , apply_url: it.apply_url ?? , apply_email: it.apply_email ?? , description: it.description ?? , }); } } catch (e) { setError(e.message || ); } })(); } else if (open) { setForm({ title: , category: , status: , location: , experience: , salary: , skills: , apply_url: , apply_email: , description: , }); } }, [open, editId, API_BASE]); // eslint-disable-line async function save() { setSaving(true); setError( ); try { if (!form.title.trim()) { setError( ); return; } const url = editId ? : ; const method = editId ? : ; const { data } = await authFetch(url, { method, body: JSON.stringify(form) }); if (data?.error) { setError(data.error); return; } onSaved?.(); onClose?.(); } catch (e) { setError(e.message || ); } finally { setSaving(false); } } if (!open) return null; return (    e.stopPropagation()}>    {editId ? : }   ✕         Title  setForm(f=>({...f,title:e.target.value}))} />    Category  setForm(f=>({...f,category:e.target.value}))}>  Full-time   Internship       Status  setForm(f=>({...f,status:e.target.value}))}>  Draft   Published   Archived       Location  setForm(f=>({...f,location:e.target.value}))} />    Experience  setForm(f=>({...f,experience:e.target.value}))} />    Salary  setForm(f=>({...f,salary:e.target.value}))} />    Skills (comma separated)  setForm(f=>({...f,skills:e.target.value}))} />    Apply URL  setForm(f=>({...f,apply_url:e.target.value}))} />    Apply Email  setForm(f=>({...f,apply_email:e.target.value}))} />    Description  setForm(f=>({...f,description:e.target.value}))} />     {error &&  {error} }      Cancel    {saving ? :"
  },
  {
    "title": "PreviewModal",
    "path": "/admin/components/PreviewModal.jsx",
    "text": "{ try { const data = await authFetch(`${apiBase}/broadcasts/${id}`); if (data?.ok) setItem(data.item); } catch (e) { console.error(e); } })(); }, [apiBase, id]); function handleKeyDown(e) { if (e.key === \"Escape\") onClose(); } return ( Preview × {!item ? ( Loading… ) : ( {item.title} { const apiOrigin = apiBase?.startsWith(\"http\") ? new URL(apiBase).origin : window.location.origin; const imgSrc = item.image_url.startsWith(\"http\") ? item.image_url : `${apiOrigin}${item.image_url}`; return ; })()} {item.link_url && ( {item.link_url} )} )} react ../admin-css/modals/base.css ../admin-css/modals/preview.css Content-Type application/json Bearer ${token} HTTP ${res.status} ${apiBase}/broadcasts/${id} Escape modal-backdrop preview-modal dialog true modal-card modal-header preview-title modal-close Close modal-content preview preview-title preview-title http http ${apiOrigin}${item.image_url} preview-media src={imgSrc} />; })()}   {item.link_url && (    "
  },
  {
    "title": "VideoFormModal",
    "path": "/admin/components/VideoFormModal.jsx",
    "text": "null); if (!res.ok) throw new Error(data?.error || `HTTP ${res.status}`); onSaved?.(data); } catch (e) { alert(e.message || \"Save failed\"); } finally { setSaving(false); } } if (!open) return null; return ( {isEdit ? `Edit Video #${editing.id}` : \"Add Video\"} ✕ Title * Top Category {t.label} )} {!!subcatOptions.length && ( Sub-category — None — {sc.name} )} )} YouTube URL Status Draft Published Description {/* Live preview of derived thumb */} {(form.thumbnail_url || derivedThumb) ? ( Thumbnail preview ) : null} Cancel {saving ? \"Saving…\" : (isEdit ? \"Save Changes\" : \"Create\")} react /api marche_admin_auth ; } catch { return ; } }; const authHeaders = () => { const t = getToken(); return t ? { Authorization: } : {}; }; const TOPS = [ { key: , label: }, { key: , label: }, { key: , label: }, ]; // build a thumbnail from a YouTube URL function youTubeThumb(url) { if (!url) return ; const m = String(url).match( /(?:https?:\\/\\/)?(?:www\\.)?(?:youtube\\.com\\/watch\\?v=|youtu\\.be\\/)([\\w-]{11})/ ); const id = m?.[1]; return id ? : ; } export default function VideoFormModal({ open = true, initialTopCat = , editing = null, onClose, onSaved, }) { const [form, setForm] = useState({ title: , top_category: initialTopCat, sub_category_id: , youtube_url: , // thumbnail_url is derived; keep a field only for preview/override if needed thumbnail_url: , status: , description: , }); const [saving, setSaving] = useState(false); const isEdit = !!editing?.id; const [subcatOptions, setSubcatOptions] = useState([]); // Prefill on open / edit useEffect(() => { if (editing) { setForm({ title: editing.title ?? , top_category: editing.top_category ?? initialTopCat, sub_category_id: editing.sub_category_id ?? , youtube_url: editing.youtube_url ?? , thumbnail_url: editing.thumbnail_url ?? , status: editing.status ?? , description: editing.description ?? , }); } else { setForm((f) => ({ ...f, top_category: initialTopCat })); } }, [editing, initialTopCat]); // Load subcategories for currently selected top category useEffect(() => { async function load() { try { const res = await fetch( , { credentials: , headers: { ...authHeaders() } } ); if (!res.ok) throw new Error( ); const data = await res.json(); setSubcatOptions(Array.isArray(data?.items) ? data.items : []); } catch { setSubcatOptions([]); } } if (form.top_category) load(); }, [form.top_category]); // keep a live derived thumbnail for preview when youtube_url changes const derivedThumb = useMemo(() => youTubeThumb(form.youtube_url), [form.youtube_url]); const update = (k, v) => setForm((f) => ({ ...f, [k]: v })); async function submit() { if (!form.title.trim()) { alert( ); return; } if (![ surgery training Invalid top category pending , }; const url = isEdit ? : ; const method = isEdit ? : ; const res = await fetch(url, { method, credentials: , headers: { application/json HTTP ${res.status} Save failed modal-backdrop modal modal-header Edit Video #${editing.id} Add Video icon-btn modal-body flex-row wrap 1 1 320px input title input top_category input } onChange={(e)=>update( , e.target.value)} >  — None —  {subcatOptions.map(sc =>  {sc.name} )}     )}    YouTube URL   update( , e.target.value)} placeholder= />      Status   update( , e.target.value)}>  Draft   Published         Description   update( , e.target.value)} />   {/* Live preview of derived thumb */} {(form.thumbnail_url || derivedThumb) ? (    Thumbnail preview      ) : null}      Cancel    {saving ? : (isEdit ? :"
  },
  {
    "title": "VideoSubcatsModal",
    "path": "/admin/components/VideoSubcatsModal.jsx",
    "text": "{ load(); }, [topCat]); return ( Manage {topCat[0].toUpperCase()+topCat.slice(1)} Sub-categories × Add {loading ? ( Loading… ) : items.length === 0 ? ( No sub-categories yet. ) : ( ( {i.name} Rename Delete ))} )} Close react /api/admin marche_admin_auth ; } catch { return ; } }; const authHeaders = () => { const t = getToken(); return t ? { Authorization: } : {}; }; export default function VideoSubcatsModal({ topCat, onClose, onChanged }) { const [items, setItems] = useState([]); const [newName, setNewName] = useState( ); const [loading, setLoading] = useState(false); async function load() { setLoading(true); try { const res = await fetch( , { credentials: , headers: { ...authHeaders() } } ); const data = await res.json(); setItems(Array.isArray(data?.items) ? data.items : []); } finally { setLoading(false); } } async function add() { const name = newName.trim(); if (!name) return; const res = await fetch( , { method: , headers: { application/json include Add failed ); await load(); onChanged?.(); } async function rename(id, name) { const v = prompt( , name); if (!v || v.trim() === name) return; const res = await fetch( , { method: , headers: { application/json include Rename failed Delete this sub-category? ${API_BASE}/video_subcategories/${id} DELETE include Delete failed modal-backdrop modal-card modal-header icon modal-content flex input New sub-category Enter btn primary empty empty table trow tcell-title row-actions btn ghost btn danger modal-footer btn secondary"
  },
  {
    "title": "index",
    "path": "/admin/index.js",
    "text": "./AdminLayout ./VideosAdmin ./CareersAdmin ./BroadcastsAdmin ./TestimonialsAdmin"
  },
  {
    "title": "TestimonialsAdmin",
    "path": "/admin/TestimonialsAdmin.jsx",
    "text": "{ try { return new URL(p).href; } catch { return p ? `${API_ORIGIN}${p.startsWith('/') ? p : `/${p}`}` : ''; } }; function TestimonialRow({ row, onEdit, onDelete, onToggle }) { return ( {row.image ? : ? } {row.person_name} {[row.person_title, row.company].filter(Boolean).join(\" • \")} {row.message} {row.is_active ? \"Active\" : \"Hidden\"} {row.is_active ? \"Hide\" : \"Activate\"} Edit Delete ({ ...prev, imageFile: f, imagePreview: f ? URL.createObjectURL(f) : prev.imagePreview })); } async function save() { const fd = new FormData(); fd.set(\"person_name\", form.person_name); fd.set(\"person_title\", form.person_title || \"\"); fd.set(\"company\", form.company || \"\"); fd.set(\"message\", form.message); fd.set(\"is_active\", String(form.is_active ? 1 : 0)); fd.set(\"linkedin\", form.linkedin || \"\"); if (form.imageFile) fd.set(\"image\", form.imageFile); try { if (form.id) { const data = await authFetch(`${API_BASE}/testimonials/${form.id}`, { method: 'PUT', body: fd, isMultipart: true }); if (!data || data.ok === false) { alert('Save failed'); return; } } else { const data = await authFetch(`${API_BASE}/testimonials`, { method: 'POST', body: fd, isMultipart: true }); if (!data || data.ok === false) { alert('Create failed'); return; } } onSaved && onSaved(); } catch (e) { console.error(e); alert('Save failed'); } } if (!open) return null; return ( {form.id ? \"Edit Testimonial\" : \"New Testimonial\"} × Person Name Title/Role Company Message Active? Yes No Image {form.imagePreview ? : null} LinkedIn URL Cancel Save { load(1); }, [q, status]); function openNew() { setEditRow(null); setOpenForm(true); } function openEdit(r) { setEditRow(r); setOpenForm(true); } async function deleteRow(r) { if (!confirm(`Delete testimonial by \"${r.person_name}\"?`)) return; const data = await authFetch(`${API_BASE}/testimonials/${r.id}`, { method: 'DELETE' }); if (!data || data.ok === false) { alert(data?.error || 'Delete failed'); return; } load(); } async function toggleRow(r) { const data = await authFetch(`${API_BASE}/testimonials/${r.id}/active`, { method: 'PATCH', body: JSON.stringify({ is_active: !r.is_active }) }); if (!data || data.ok === false) { alert(data?.error || 'Update failed'); return; } load(); } const totalPages = Math.max(1, Math.ceil(total / limit)); return ( All Active only Hidden + New Testimonial Image Person Status Actions {loading ? Loading… ( )) : No results } Prev {page} / {totalPages} Next react ../../components/SectionHeader ../../auth/AuthContext ./admin-css/admin.css ./admin-css/modals/base.css ./admin-css/modals/testimonial.css http://localhost:8080/api/admin ${API_ORIGIN}${p.startsWith( ) ? p : : ; } }; function TestimonialRow({ row, onEdit, onDelete, onToggle }) { return (     {row.image ?   :  ? }      {row.person_name}    {[row.person_title, row.company].filter(Boolean).join( )}    {row.message}       {row.is_active ? : }       onToggle(row)} className= >{row.is_active ? : }   onEdit(row)} className= >Edit   onDelete(row)} className= >Delete      ); } function TestimonialFormModal({ open, initial, onClose, onSaved, authFetch }) { const [form, setForm] = useState({ id: 0, person_name: , person_title: , company: , message: , is_active: 1, imageFile: null, imagePreview: , linkedin: }); useEffect(()=>{ if (open) setForm({ id: initial?.id || 0, person_name: initial?.person_name || , person_title: initial?.person_title || , company: initial?.company || , message: initial?.message || , is_active: initial?.is_active ? 1 : 0, imageFile: null, imagePreview: initial?.image || , linkedin: initial?.linkedin || }); }, [open, initial]); function onPickFile(e) { const f = e.target.files?.[0] || null; setForm(prev => ({ ...prev, imageFile: f, imagePreview: f ? URL.createObjectURL(f) : prev.imagePreview })); } async function save() { const fd = new FormData(); fd.set( , form.person_name); fd.set( , form.person_title || ); fd.set( , form.company || ); fd.set( , form.message); fd.set( , String(form.is_active ? 1 : 0)); fd.set( , form.linkedin || ); if (form.imageFile) fd.set( , form.imageFile); try { if (form.id) { const data = await authFetch( , { method: , body: fd, isMultipart: true }); if (!data || data.ok === false) { alert( ); return; } } else { const data = await authFetch( , { method: , body: fd, isMultipart: true }); if (!data || data.ok === false) { alert( ); return; } } onSaved && onSaved(); } catch (e) { console.error(e); alert( ); } } if (!open) return null; return (    e.stopPropagation()}>    {form.id ? : }   ×       Person Name setForm(f=>({...f, person_name:e.target.value}))} />   Title/Role setForm(f=>({...f, person_title:e.target.value}))} />   Company setForm(f=>({...f, company:e.target.value}))} />   Message setForm(f=>({...f, message:e.target.value}))} />   Active?  setForm(f=>({...f, is_active:Number(e.target.value)}))}>  Yes   No       Image     {form.imagePreview ?   : null}  LinkedIn URL   setForm(f => ({ ...f, linkedin: e.target.value }))} placeholder= />        Cancel   Save        ); } export default function TestimonialsAdmin() { const { token } = useAuth(); const [q, setQ] = useState( ); const [status, setStatus] = useState( ); const [rows, setRows] = useState([]); const [page, setPage] = useState(1); const [total, setTotal] = useState(0); const [limit] = useState(20); const [loading, setLoading] = useState(false); const [openForm, setOpenForm] = useState(false); const [editRow, setEditRow] = useState(null); const authFetch = useMemo(()=>async (url, init={})=>{ const headers = new Headers(init.headers || {}); if (!init.isMultipart && init.body && !(init.body instanceof FormData)) { headers.set( application/json Authorization Bearer ${token} include content-type ; if (ct.includes( )) { const data = await res.json().catch(()=>null); // normalize shape so callers can rely on .ok return { ok: res.ok, status: res.status, ...data }; } // No JSON body → still report HTTP ok return { ok: res.ok, status: res.status }; }, [token]); async function load(p = page) { setLoading(true); try { const u = new URL( , window.location.origin); if (q) u.searchParams.set( , q); if (status) u.searchParams.set( , status); u.searchParams.set( , String(p)); u.searchParams.set( , String(limit)); const data = await authFetch(u.toString()); setRows(data?.items || []); setTotal(data?.total || 0); setPage(data?.page || p); } finally { setLoading(false); } } useEffect(()=>{ load(1); }, [q, status]); function openNew() { setEditRow(null); setOpenForm(true); } function openEdit(r) { setEditRow(r); setOpenForm(true); } async function deleteRow(r) { if (!confirm( ${r.person_name} )) return; const data = await authFetch( , { method: }); if (!data || data.ok === false) { alert(data?.error || ); return; } load(); } async function toggleRow(r) { const data = await authFetch( , { method: , body: JSON.stringify({ is_active: !r.is_active }) }); if (!data || data.ok === false) { alert(data?.error || ); return; } load(); } const totalPages = Math.max(1, Math.ceil(total / limit)); return (          setQ(e.target.value)} placeholder= />  setStatus(e.target.value)}>  All   Active only   Hidden     + New Testimonial           Image   Person   Status   Actions      {loading ?  Loading…  : rows.length ? rows.map(r=>(   )) :  No results }      "
  },
  {
    "title": "VideosAdmin",
    "path": "/admin/VideosAdmin.jsx",
    "text": "window.removeEventListener(\"admin:search\", onAdminSearch); }, []); return ( t.key === topCat)?.label}{\" \"} Sub-categories + Add Video ( {c.label} ))} {!!subcats.length && ( All ( {sc.name} ))} )} ID Thumbnail Title Top Cat. Sub-cat. Status Actions {loading ? ( Loading… ) : rows.length === 0 ? ( No videos found. ( {row.id} {row.title} {row.top_category} {row.sub_category_name || \"—\"} {row.status} View Edit Delete )) )} {showSubcatModal && ( )} {showForm && ( )} react ./components/VideoSubcatsModal ./components/VideoFormModal ../../components/SectionHeader ../../utils/youtube ./admin-css/admin.css /api marche_admin_auth ; } catch { return ; } }; const authHeaders = () => { const t = getToken(); return t ? { Authorization: } : {}; }; const TOP_CATEGORIES = [ { key: , label: }, { key: , label: }, { key: , label: }, ]; export default function VideosAdmin() { const [topCat, setTopCat] = useState( ); const [subcats, setSubcats] = useState([]); const [subcat, setSubcat] = useState( ); const [query, setQuery] = useState( ); const [rows, setRows] = useState([]); const [loading, setLoading] = useState(false); const [showSubcatModal, setShowSubcatModal] = useState(false); const [showForm, setShowForm] = useState(false); const [editing, setEditing] = useState(null); async function loadSubcats() { try { const res = await fetch( , { credentials: , headers: { ...authHeaders() } } ); if (!res.ok) throw new Error( ); const data = await res.json(); setSubcats(Array.isArray(data?.items) ? data.items : []); } catch (e) { console.error( , e); setSubcats([]); } } async function loadVideos() { setLoading(true); try { const url = new URL( , window.location.origin); url.searchParams.set( , topCat); if (subcat !== ) url.searchParams.set( , subcat); if (query) url.searchParams.set( , query); const res = await fetch(url, { credentials: , headers: { ...authHeaders() }, }); if (!res.ok) throw new Error( ); const data = await res.json(); setRows(Array.isArray(data?.items) ? data.items : []); } catch (e) { console.error( , e); setRows([]); } finally { setLoading(false); } } async function remove(id) { if (!confirm( )) return; const res = await fetch( , { method: , headers: { ...authHeaders() }, credentials: , }); if (!res.ok) { const t = await res.text(); alert(t || ); return; } loadVideos(); } useEffect(() => { loadSubcats(); }, [topCat]); useEffect(() => { loadVideos(); }, [topCat, subcat, query]); // global search (from topbar Ctrl/⌘+K) useEffect(() => { function onAdminSearch(e) { setQuery(e.detail?.q ?? ); } window.addEventListener( , onAdminSearch); return () => window.removeEventListener( , onAdminSearch); }, []); return ( <>           setShowSubcatModal(true)} > Manage {TOP_CATEGORIES.find((t) => t.key === topCat)?.label}{ } Sub-categories     { setEditing(null); setShowForm(true); }} > + Add Video         {TOP_CATEGORIES.map((c) => (   { setTopCat(c.key); setSubcat( ); }} > {c.label}   ))}   {!!subcats.length && (     setSubcat( )} > All   {subcats.map((sc) => (      {row.title}   {row.top_category}   {row.sub_category_name || }   {row.status}      window.open(row.youtube_url || row.src_url || _blank btn ghost btn danger"
  },
  {
    "title": "App",
    "path": "/App.js",
    "text": "{/* ✅ Add this */} react-router-dom ./pages/careers ./pages/careersapply ./SearchResults element={ } />  } />  } /> {/* ✅ Add this */}  "
  },
  {
    "title": "Careers",
    "path": "/Careers.jsx",
    "text": "{ if (activeJob === id) setActiveJob(null); else setActiveJob(id); }; return ( {/* Banner */} Careers at Marche Healthcare Join our mission and grow with us {/* Expectations Section */} What We Expect at Marche Healthcare We don’t just want employees who “fit a job description.” We want individuals who bring: At Marche Healthcare, we don’t just hire employees – we nurture changemakers who share our vision of creating a healthier future through innovation, compassion, and technology. We strongly believe that healthcare is not just about treatment — it’s about people’s lives, their families, and their trust. That’s why we expect every member of our team to embrace our core values of empathy, integrity, and excellence in everything they do. Our Expectations Passion for Healthcare Innovation: Curiosity, creativity, and drive to design impactful solutions. Commitment to Excellence: Precision, quality, and accountability in all work. Teamwork and Collaboration: Open communication and positive contribution to the team. Ethics and Responsibility: Uphold trust, privacy, and ethical practices. Adaptability and Continuous Learning: Be flexible, learn constantly, embrace change. {/* Current Openings */} Current Openings {/* Toggle */} ( {label} ))} {/* Jobs */} {loading && Loading… } {!loading && err && {err} { const faqObj = { header: job.title, id: job.id, text: job.description || \"\", }; return ( Apply ); })} ./css/careers.css react ../Layouts/Layouts react-router-dom ../components/AccordionItem /api sample-1 Software Engineer fulltime Build and maintain web apps with scalable backend services, integrate APIs, and collaborate with cross-functional teams. Remote 1–3 yrs ₹3–5 LPA React, Node.js, SQL Full Time intern internship fulltime Full Time ); const [activeJob, setActiveJob] = useState(null); const navigate = useNavigate(); // fetch published jobs from public API useEffect(() => { let alive = true; (async () => { try { setLoading(true); setErr( ); const category = uiToApiCategory(selectedCategory); const url = new URL( , window.location.origin); url.searchParams.set( , category); url.searchParams.set( ); const res = await fetch(url.toString(), { cache: }); const data = await res.json(); if (!data?.ok) throw new Error(data?.error || ); if (alive) setRows(Array.isArray(data.items) ? data.items : []); } catch (e) { if (alive) { setErr(e.message || ); setRows([]); } } finally { if (alive) setLoading(false); } })(); return () => { alive = false; }; }, [selectedCategory]); const jobs = useMemo( () => (rows.length ? rows : SAMPLE_JOBS), [rows] ); const handleApply = (job) => { // if backend gives an external application URL, go there if (job.apply_url) { window.open(job.apply_url, noopener,noreferrer /careersapply Careers - Marche Healthcare banner banner-text expectations-section py-20 px-6 md:px-20 expectations-grid md:flex md:items-center md:gap-10 expectations-text md:w-1/2 text-3xl font-bold text-[#0c1e3a] mb-4 text-xl font-semibold text-[#0c1e3a] mb-4 text-gray-600 mb-4 text-gray-600 mb-4 font-semibold mb-2 list-disc ml-5 text-gray-600 expectations-video md:w-1/2 mt-6 md:mt-0 /careers/video.mp4 expectations-video-file current-opening toggle-buttons Full Time Internship active } onClick={() => setSelectedCategory(label)} > {label}   ))}   {/* Jobs */}   {loading &&  Loading… } {!loading && err &&  {err} } {!loading && jobs.map((job) => { const faqObj = { header: job.title, id: job.id, text: job.description || , }; return (   setActiveJob(job.id)} onMouseLeave={() => setActiveJob(null)} >    "
  },
  {
    "title": "careersapply",
    "path": "/careersapply.jsx",
    "text": "{ e.preventDefault(); if (!formData.resumeFile) { alert(\"❌ Please upload your resume (PDF only, max 5 MB).\"); return; } const formDataToSend = new FormData(); formDataToSend.append(\"name\", formData.name); formDataToSend.append(\"email\", formData.email); formDataToSend.append(\"phone\", formData.phone); formDataToSend.append(\"message\", formData.message); formDataToSend.append(\"resume\", formData.resumeFile); try { const res = await fetch(`${API_BASE}/apply`, { method: \"POST\", body: formDataToSend, }); const data = await res.json(); if (data.success) { alert(\"✅ Application submitted successfully!\"); setFormData({ name: \"\", email: \"\", phone: \"\", resumeFile: null, message: \"\", }); } else { alert(\"❌ \" + (data.error || \"Failed to submit\")); } } catch (err) { console.error(err); alert(\"❌ Failed to connect to server\"); } }; return ( Apply — {jobTitle} Submit your resume Skills Matter, No Stories! Showcase your capabilities and measurable results. Name Email Mobile Number Upload Resume (PDF only, max 5MB) Message Submit react-router-dom react framer-motion ./css/careersapply.css ../Layouts/Layouts http://localhost:8080/api , email: , phone: , resumeFile: null, message: , }); const jobTitle = useMemo(() => job?.title || , [job]); const handleChange = (e) => setFormData({ ...formData, [e.target.id]: e.target.value }); const handleFileChange = (e) => { const file = e.target.files[0]; if (file) { if (file.type !== ) { alert( ); e.target.value = ; return; } if (file.size > 5 * 1024 * 1024) { alert( ); e.target.value = ; return; } setFormData({ ...formData, resumeFile: file }); } }; const handleSubmit = async (e) => { e.preventDefault(); if (!formData.resumeFile) { alert( ); return; } const formDataToSend = new FormData(); formDataToSend.append( , formData.name); formDataToSend.append( , formData.email); formDataToSend.append( , formData.phone); formDataToSend.append( , formData.message); formDataToSend.append( , formData.resumeFile); try { const res = await fetch( , { method: , body: formDataToSend, }); const data = await res.json(); if (data.success) { alert( ); setFormData({ name: , email: , phone: , resumeFile: null, message: , }); } else { alert( + (data.error || )); } } catch (err) { console.error(err); alert( ); } }; return (        Apply — {jobTitle}   Submit your resume             Skills Matter, No Stories!   Showcase your capabilities and measurable results.       Name         Email         Mobile Number         Upload Resume (PDF only, max 5MB)         Message       Submit       "
  },
  {
    "title": "ContactPage",
    "path": "/ContactPage.jsx",
    "text": "{ event.preventDefault(); // Basic validation if (!name || !email || !purpose || !message) { setSubmitStatus(\"Please fill out all required fields.\"); return; } // --- Submission Logic --- // This section is where you'd integrate your actual form submission. // I've kept the fetch example as it's a common approach for backend APIs. // If you're using a service like EmailJS, you'd replace this block. try { const res = await fetch(\"http://localhost:8080/api/contact\", { method: \"POST\", headers: { \"Content-Type\": \"application/json\" }, body: JSON.stringify({ name, email, purpose, message }), }); const data = await res.json(); if (res.ok) { setSubmitStatus(\"Message sent successfully!\"); // Reset form fields on successful submission setName(\"\"); setEmail(\"\"); setPurpose(\"\"); setMessage(\"\"); } else { setSubmitStatus(data.error || \"Failed to send message. Please try again.\"); } } catch (err) { console.error(\"Submission error:\", err); setSubmitStatus(\"Network error: Could not send message. Please check your connection.\"); } // --- End Submission Logic --- }; const cardDetails = [ { img: ( ), header: \"Email\", content: \"Hi, our team is here to assist you, just mail us!\", link: \"info@marchehealthcare.org\", }, { img: ( ), header: \"Join our Community\", content: \"Stay informed with our latest news and events. Follow us to be part of the conversation\", link: [ { lin: \"https://www.linkedin.com/company/marche-healthcare/\", img: ( ), }, { lin: \"https://x.com/info_march49738\", img: ( ), }, { lin: \"https://www.instagram.com/invites/contact/?igsh=jcoo1221g882&utm_content=uh81aej\", img: ( ), }, { lin: \"https://youtube.com/@marchehealthcare?si=Nktj1VOLNtZBrdmS\", img: ( ), }, ], }, { img: ( ), header: \"Locate us\", content: \"Find our office and location to reach us\", link: { url: \"https://maps.app.goo.gl/hQk1K5rY443aQ5rRA\", // Updated with a more direct Google Maps link text: \"13, Near Auroville, Pondicherry-Tindivanam highways, Thiruchitrambalam, Vanur, Tamilnadu-605111\", }, }, ]; return ( Contact us Kindly submit your details and share your message to us, thankyou ( ))} Hello! Feel free to get in touch with us Discover how we can support your needs by reaching out to us. We're here to provide assistance and guidance. Name Email Purpose Select a purpose Request a Demo Purchase Find a Distributor Meeting Others Detailed message about selected purpose: {purpose || \"N/A\"} Submit {submitStatus && {submitStatus} } react framer-motion ./css/contactpage.css ../Layouts/Layouts ../components/ContactCards ); const [email, setEmail] = useState( ); const [purpose, setPurpose] = useState( ); const [message, setMessage] = useState( ); const [submitStatus, setSubmitStatus] = useState(null); // For success/error feedback const handleSubmit = async (event) => { event.preventDefault(); // Basic validation if (!name || !email || !purpose || !message) { setSubmitStatus( ); return; } // --- Submission Logic --- // This section is where you ve kept the fetch example as it re using a service like EmailJS, you http://localhost:8080/api/contact POST Content-Type application/json Message sent successfully! ); setEmail( ); setPurpose( ); setMessage( ); } else { setSubmitStatus(data.error || ); } } catch (err) { console.error( , err); setSubmitStatus( ); } // --- End Submission Logic --- }; const cardDetails = [ { img: (                  ), header: , content: , link: , }, { img: (                  ), header: , content: , link: [ { lin: , img: (       ), }, { lin: , img: (       ), }, { lin: , img: (       ), }, { lin: , img: (       ), }, ], }, { img: (                  ), header: , content: , link: { url: , // Updated with a more direct Google Maps link text: , }, }, ]; return (        Contact us    Kindly submit your details and share your message to us, thankyou         {cardDetails.map((card) => (   ))}          Hello! Feel free to get in touch with us    Discover how we can support your needs by reaching out to us. We name text name required email email email required purpose purpose required disabled> Select a purpose    Request a Demo   Purchase   Find a Distributor   Meeting   Others          Detailed message about selected purpose: {purpose || }     setMessage(event.target.value)} required // Added />    Submit  {submitStatus &&  {submitStatus} }      "
  },
  {
    "title": "HomePage",
    "path": "/HomePage.jsx",
    "text": "{ if (window.innerWidth 768 && window.innerWidth { setIsMobile(window.innerWidth { if (!window.customElements || !window.customElements.get('model-viewer')) { const s = document.createElement('script'); s.type = 'module'; s.src = 'https://unpkg.com/@google/model-viewer/dist/model-viewer.min.js'; s.async = true; document.head.appendChild(s); } }, []); return ( {/* Keep only YouTube player in the value-proposition section */} {/* 3D Model Section */} {/* */} Marche’s Values Your browser does not support the video tag. At the heart of our mission, our values guide us in every step. Discover the principles that drive our commitment to improving lives globally Innovation Pushing Boundaries, Bridging Health Disparities Collaboration Unite diverse expertise to create impactful innovations Accessibility Bringing Modern Healthcare to Everyone Compassion Empathy at Our Core Enhancing Lives Worldwide {/* testmonial */} {/* Duplicate logos for infinite scroll effect (kept as-is) */} {bcLoading ? ( // optional loading state Loading latest broadcast... { const imageSrc = resolveImageUrl(broadcast.image_url) || \"./home/surgery.jpg\"; const title = broadcast.title || \"Untitled Broadcast\"; const summary = broadcast.summary || (broadcast.body_html ? String(broadcast.body_html).replace(/ /g, '') : \"\"); const dateStr = broadcast.created_at ? new Date(broadcast.created_at).toLocaleDateString() : \"\"; const externalLink = broadcast.link_url && /^https?:\\/\\//i.test(broadcast.link_url) ? broadcast.link_url : null; const cardHref = externalLink || \"/news\"; return ( {title} Marche Healthcare {dateStr} {summary} Read more... show default card (keeps previous design) Blog title heading will go here Marche Healthcare 12-12-2022 Lorem ipsum dolor, sit amet consectetur adipisicing elit. A labore eos hic, doloremque quasi sed! Et enim debitis alias non perferendis labore voluptas tenetur, veniam placeat iusto culpa officia ea. Read more... )} More Update react ./css/homepage.css ../utils/SplitStringUsingRegex framer-motion ../Layouts/Layouts ../components/SectionHeader ../assets/home/author.png ../components/Carousel ../components/CaruselTwo ../components/NewsCard ../components/SwiperSingle ../components/YoutubeVideoPlayer ../components/ModelViewer3D react-router-dom /api ./card1.png Blog title heading will go here Lorem ipsum dolor sit amet, consectetur adipiscing elit. Suspendisse varius enim in eros ./companyLogo.png Full name1 11 Jan 2022 https://www.linkedin.com/company/marche-healthcare/ card2.png Blog title heading will go here Lorem ipsum dolor sit amet, consectetur adipiscing elit. Suspendisse varius enim in eros ./companyLogo.png Full name2 11 Jan 2022 https://www.linkedin.com/company/marche-healthcare/ /news easeInOutQuart re on mobile const [isMobile, setIsMobile] = useState(false); // Add ref for the swiper container const swiperRef = useRef(null); const [slideState, setSlideState] = useState({ noOfSlide: 3, navigation: true, }); // New: broadcast state (latest published social broadcast) const [broadcast, setBroadcast] = useState(null); const [bcLoading, setBcLoading] = useState(true); useEffect(() => { const handleResize = () => { if (window.innerWidth   ({ ...prevState, noOfSlide: 1, navigation: false, })); setIsMobile(true); } else if (window.innerWidth > 768 && window.innerWidth   ({ ...prevState, noOfSlide: 2, navigation: false, })); setIsMobile(false); } else { setSlideState((prevState) => ({ ...prevState, noOfSlide: 3, navigation: true, })); setIsMobile(false); } }; // Add mobile detection const checkMobile = () => { setIsMobile(window.innerWidth   { window.removeEventListener( , handleResize); window.removeEventListener( , checkMobile); }; }, []); // Fetch latest broadcast (category=social, limit=1) useEffect(() => { let mounted = true; async function loadLatestBroadcast() { try { const res = await fetch( , { cache: }); const data = await res.json(); if (!mounted) return; if (data?.ok && Array.isArray(data.items) && data.items.length > 0) { setBroadcast(data.items[0]); } else { setBroadcast(null); } } catch (err) { console.error( , err); setBroadcast(null); } finally { if (mounted) setBcLoading(false); } } loadLatestBroadcast(); return () => { mounted = false; }; }, []); // helper to resolve image URL (absolute when needed) function resolveImageUrl(url) { if (!url) return null; if (/^https?:\\/\\//i.test(url)) return url; // Use the API_BASE URL to correctly resolve the image path const apiOrigin = new URL(API_BASE, window.location.origin).origin; return ; } useEffect(() => { if (!window.customElements || !window.customElements.get( )) { const s = document.createElement( ); s.type = ; s.src = ; s.async = true; document.head.appendChild(s); } }, []); return (         {/* Keep only YouTube player in the value-proposition section */}       {/* 3D Model Section */} {/*       */}    Marche’s Values        Your browser does not support the video tag.         At the heart of our mission, our values guide us in every step. Discover the principles that drive our commitment to improving lives globally                    Innovation    Pushing Boundaries, Bridging Health Disparities                  Collaboration    Unite diverse expertise to create impactful innovations                  Accessibility    Bringing Modern Healthcare to Everyone                  Compassion    Empathy at Our Core Enhancing Lives Worldwide                 {/* testmonial */}                                       {/* Duplicate logos for infinite scroll effect (kept as-is) */}                                             {bcLoading ? ( // optional loading state        Loading latest broadcast...        ) : broadcast ? ( (() => { const imageSrc = resolveImageUrl(broadcast.image_url) || ; const title = broadcast.title || ; const summary = broadcast.summary || (broadcast.body_html ? String(broadcast.body_html).replace(/ ]+>/g, ) : ); const dateStr = broadcast.created_at ? new Date(broadcast.created_at).toLocaleDateString() : ; const externalLink = broadcast.link_url && /^https?:\\/\\//i.test(broadcast.link_url) ? broadcast.link_url : null; const cardHref = externalLink || ; return (            {title}               Marche Healthcare     {dateStr}                  {summary}  Read more...            ); })() ) : ( // no broadcast found -> show default card (keeps previous design)            Blog title heading will go here               Marche Healthcare     12-12-2022                  Lorem ipsum dolor, sit amet consectetur adipisicing elit. A labore eos hic, doloremque quasi sed! Et enim debitis alias non perferendis labore voluptas tenetur, veniam placeat iusto culpa officia ea.  Read more...            )}      "
  },
  {
    "title": "NewsPage",
    "path": "/NewsPage.jsx",
    "text": "); const twitterSvg = ( ); const instagramSvg = ( ); const youtubeSvg = ( { if (window.innerWidth { active = false; }; }, []); const SECTION_DATA = { socialmedia: { id: \"socialmedia\", title: \"Social Media\", content: ( {linkedInSvg} {twitterSvg} {instagramSvg} {youtubeSvg} ` ${index + 1} {spLoading && ( Loading posts… )} {!spLoading && spError && ( {spError}. Showing sample card below. 0 ? { id: p.id ?? idx, img: abs(p.image_url || \"\"), // from broadcasts.image_url title: p.title || \"\", content: p.summary || \"\", // use summary; body_html is for detail pages profile: \"./companyLogo.png\", names: (p.social_source ? p.social_source[0].toUpperCase() + p.social_source.slice(1) : \"Marche Healthcare\"), date: (p.scheduled_at || p.created_at || \"\").slice(0,10), link: p.link_url || \"\", social_source: p.social_source || null, } : p; return ( {card.link ? ( ) : ( )} ); })} ), }, newsandevents: { id: \"newsandevents\", title: \"News & Events\", content: ( {visibleSwiper ? ( ` ${i + 1} {newsLoading && ( Loading news… )} {!newsLoading && newsError && ( {newsError}. Showing sample card below. { const item = dbNewsPosts.length ? { id: p.id ?? idx, img: abs(p.image_url || \"\"), title: p.title || \"\", content: p.summary || \"\", body_html: p.body_html || \"\", profile: \"./companyLogo.png\", names: \"Marche Healthcare\", date: (p.scheduled_at || p.created_at || \"\").slice(0,10), } : p; return ( ); })} ) : null} {activeCard?.isActive ? ( ) : null} ), }, }; return ( Broadcast Stay updated with our latest innovations and initiatives on our Broadcast. Join us at upcoming events to collaborate and drive health equality&nbsp;forward Social Media News & Events {SECTION_DATA[selectedSection].title} {SECTION_DATA[selectedSection].content} ./css/newspage.css react-router-dom ../Layouts/Layouts react framer-motion swiper/react swiper/modules ../components/MediaCard ../components/NewsCard ../components/NewsFullDetails ${window.location.protocol}//${window.location.host}/api M19 0h-14c-2.76 0-5 2.24-5 5v14c0 2.76 2.24 5 5 5h14c2.76 0 5-2.24 5-5v-14c0-2.76-2.24-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.25c-.97 0-1.75-.79-1.75-1.75s.78-1.75 1.75-1.75 1.75.79 1.75 1.75-.78 1.75-1.75 1.75zm13.5 12.25h-3v-5.5c0-1.38-.56-2-1.75-2-1.14 0-1.75.79-1.75 2v5.5h-3v-11h3v1.62c.41-.79 1.27-1.62 2.75-1.62 1.94 0 3.5 1.12 3.5 4.01v6.99z M24 4.56c-.89.39-1.84.65-2.84.77a4.92 4.92 0 0 0 2.17-2.71c-.94.56-1.97.96-3.08 1.18a4.89 4.89 0 0 0-8.32 4.45c-4.07-.2-7.68-2.15-10.1-5.1a4.87 4.87 0 0 0-.66 2.46c0 1.7.87 3.2 2.19 4.08-.81-.03-1.57-.25-2.23-.62v.06c0 2.38 1.69 4.36 3.93 4.81-.41.11-.84.17-1.28.17-.31 0-.61-.03-.91-.08.61 1.91 2.39 3.3 4.5 3.34a9.86 9.86 0 0 1-6.1 2.1c-.4 0-.79-.02-1.17-.07a13.94 13.94 0 0 0 7.56 2.21c9.05 0 14-7.5 14-14 0-.21 0-.42-.01-.63a9.9 9.9 0 0 0 2.45-2.53z M12 2.16c3.2 0 3.58.01 4.85.07 1.17.05 1.94.24 2.39.41.59.23 1.01.51 1.46.96.45.45.73.87.96 1.46.18.45.36 1.22.41 2.39.06 1.27.07 1.65.07 4.85s-.01 3.58-.07 4.85c-.05 1.17-.24 1.94-.41 2.39-.23.59-.51 1.01-.96 1.46-.45.45-.87.73-1.46.96-.45.18-1.22.36-2.39.41-1.27.06-1.65.07-4.85.07s-3.58-.01-4.85-.07c-1.17-.05-1.94-.24-2.39-.41-.59-.23-1.01-.51-1.46-.96-.45-.45-.73-.87-.96-1.46-.18-.45-.36-1.22-.41-2.39-.06-1.27-.07-1.65-.07-4.85s.01-3.58.07-4.85c.05-1.17.24-1.94.41-2.39.23-.59.51-1.01.96-1.46.45-.45.87-.73 1.46-.96.45-.18 1.22-.36 2.39-.41 1.27-.06 1.65-.07 4.85-.07zm0-2.16c-3.29 0-3.7.01-4.99.07-1.32.06-2.23.26-3.01.57-.87.34-1.61.8-2.36 1.55-.75.75-1.21 1.49-1.55 2.36-.31.78-.51 1.69-.57 3.01-.06 1.29-.07 1.7-.07 4.99s.01 3.7.07 4.99c.06 1.32.26 2.23.57 3.01.34.87.8 1.61 1.55 2.36.75.75 1.49 1.21 2.36 1.55.78.31 1.69.51 3.01.57 1.29.06 1.7.07 4.99.07s3.7-.01 4.99-.07c1.32-.06 2.23-.26 3.01-.57.87-.34 1.61-.8 2.36-1.55.75-.75 1.21-1.49 1.55-2.36.31-.78.51-1.69.57-3.01.06-1.29.07-1.7.07-4.99s-.01-3.7-.07-4.99c-.06-1.32-.26-2.23-.57-3.01-.34-.87-.8-1.61-1.55-2.36-.75-.75-1.49-1.21-2.36-1.55-.78-.31-1.69-.51-3.01-.57-1.29-.06-1.7-.07-4.99-.07zM12 5.84c-3.4 0-6.16 2.76-6.16 6.16s2.76 6.16 6.16 6.16 6.16-2.76 6.16-6.16-2.76-6.16-6.16-6.16zm0 10.32c-2.29 0-4.16-1.87-4.16-4.16s1.87-4.16 4.16-4.16 4.16 1.87 4.16 4.16-1.87 4.16-4.16 4.16zm6.48-10.64c-.77 0-1.4-.63-1.4-1.4s.63-1.4 1.4-1.4 1.4.63 1.4 1.4-.63 1.4-1.4 1.4z M19.6 3.2c-.8-.3-2.8-.3-5.6-.3s-4.8 0-5.6.3c-.8.3-1.4.7-2 1.3-.5.5-1 1.2-1.3 2-.3.8-.3 2.8-.3 5.6s0 4.8.3 5.6c.3.8.7 1.4 1.3 2 .5.5 1.2 1 2 1.3.8.3 2.8.3 5.6.3s4.8 0 5.6-.3c.8-.3 1.4-.7 2-1.3.5-.5 1-1.2 1.3-2 .3-.8.3-2.8.3-5.6s0-4.8-.3-5.6c-.3-.8-.7-1.4-1.3-2-.5-.5-1.2-1-2-1.3zm-7.6 9.9v-5.4l4.6 2.7-4.6 2.7z linkedin twitter instagram youtube ./card1.png Sample blog Fallback content ./companyLogo.png Marche Healthcare 2025-01-01 https://www.linkedin.com/company/marche-healthcare/ ./card2.png Sample news Fallback news content ./companyLogo.png Marche Healthcare 2025-01-02 socialmedia ); const [dbNewsPosts, setDbNewsPosts] = useState([]); const [newsLoading, setNewsLoading] = useState(true); const [newsError, setNewsError] = useState( ); // 🔹 helper: make URLs absolute so images load, even if API returns const apiOrigin = (API_BASE.startsWith( ) ? new URL(API_BASE) : new URL(API_BASE, window.location.origin) ).origin; const abs = (u) => (u && /^https?:\\/\\//i.test(u) ? u : ); // 🔹 fetch News & Events broadcasts useEffect(() => { let active = true; (async () => { try { setNewsLoading(true); setNewsError( ); const res = await fetch( , { cache: }); const data = await res.json(); if (!data?.ok) throw new Error(data?.error || ); if (active) setDbNewsPosts(Array.isArray(data.items) ? data.items : []); } catch (e) { if (active) setNewsError(e.message || ); } finally { if (active) setNewsLoading(false); } })(); return () => { active = false; }; }, []); // Hash section handling useEffect(() => { if (location.hash === ) setSelectedSection( ); else if (location.hash === ) setSelectedSection( ); }, [location]); useEffect(() => { if (location.hash) { const element = document.querySelector(location.hash); if (element) element.scrollIntoView({ behavior: }); } }, [location]); // Responsive slides useEffect(() => { const handleResize = () => { if (window.innerWidth   window.removeEventListener( , handleResize); }, []); // Fetch Social Media posts from DB useEffect(() => { let active = true; (async () => { try { setSpLoading(true); setSpError( ); const res = await fetch( , { cache: }); const data = await res.json(); if (!data?.ok) throw new Error(data?.error || ); if (active) setDbSocialPosts(Array.isArray(data.items) ? data.items : []); } catch (err) { if (active) setSpError(err.message || ); } finally { if (active) setSpLoading(false); } })(); return () => { active = false; }; }, []); useEffect(() => { let active = true; (async () => { try { setNewsLoading(true); setNewsError( ); const res = await fetch( , { cache: }); const data = await res.json(); if (!data?.ok) throw new Error(data?.error || ); if (active) setDbNewsPosts(Array.isArray(data.items) ? data.items : []); } catch (e) { if (active) setNewsError(e.message || ); } finally { if (active) setNewsLoading(false); } })(); return () => { active = false; }; }, []); const SECTION_DATA = { socialmedia: { id: , title: , content: (        {linkedInSvg}       {twitterSvg}       {instagramSvg}       {youtubeSvg}          ${className} custom-bullet , }} style={{ margin: , paddingBottom: }} > {spLoading && (    Loading posts…    )} {!spLoading && spError && (     {spError}. Showing sample card below.     )} {(!spLoading && dbSocialPosts.length > 0 ? dbSocialPosts : blogCardDetails).map((p, idx) => { const card = dbSocialPosts.length > 0 ? { id: p.id ?? idx, img: abs(p.image_url || ), // from broadcasts.image_url title: p.title || , content: p.summary || , // use summary; body_html is for detail pages profile: , names: (p.social_source ? p.social_source[0].toUpperCase() + p.social_source.slice(1) : ), date: (p.scheduled_at || p.created_at || ).slice(0,10), link: p.link_url || , social_source: p.social_source || null, } : p; return (   {card.link ? (       ) : (   )}   ); })}          ), }, newsandevents: { id: , title: , content: (   {visibleSwiper ? ( <>   ${cls} custom-bullet , }} style={{ margin: , paddingBottom: }} > {newsLoading && (    Loading news…    )} {!newsLoading && newsError && (     {newsError}. Showing sample card below.     )} {(dbNewsPosts.length ? dbNewsPosts : newsCardDetails).map((p, idx) => { const item = dbNewsPosts.length ? { id: p.id ?? idx, img: abs(p.image_url || ), title: p.title || , content: p.summary || , body_html: p.body_html || , profile: , names: , date: (p.scheduled_at || p.created_at || ).slice(0,10), } : p; return (     { setVisibleSwiper(false); setSelctedCardList([{ ...item, isActive: }]); }} />   ); })}        ) : null} {activeCard?.isActive ? (   0 ? dbNewsPosts.map((p, idx) => ({ id: p.id ?? idx, img: abs(p.image_url || ), title: p.title || , content: p.summary || , profile: , names: , date: (p.scheduled_at || p.created_at || ).slice(0, 10), })) : (newsCardDetails.length ? newsCardDetails : blogCardDetails) ).filter((obj) => obj.id !== activeCard?.id) } /> ) : null}   ), }, }; return (        Broadcast    Stay updated with our latest innovations and initiatives on our Broadcast. Join us at upcoming events to collaborate and drive health equality&nbsp;forward         setSelectedSection( )} > Social Media     setSelectedSection( )} > News & Events        "
  },
  {
    "title": "ProductPage",
    "path": "/ProductPage.jsx",
    "text": "{ setActiveProducts(null); }; return ( Describe what your company does in a few words Describe exactly what the company does and what a customer can expect when working with the company. Avoid using verbose words or phrases. NovaLap 360 D8 NovaLap 360D8 is a multi-DoF articulating laparoscopic system that delivers robotic-like dexterity without robotic infrastructure. Complete lineup Your browser does not support the video tag. How functional Lorem ipsum dolor sit amet, consectetur adipiscing elit. Blog title heading will go here Lorem ipsum dolor sit amet, consectetur adipiscing elit. Suspendisse varius enim in eros. Lorem ipsum dolor sit amet, consectetur adipiscing elit. Blog title heading will go here Lorem ipsum dolor sit amet, consectetur adipiscing elit. Suspendisse varius enim in eros. Lorem ipsum dolor sit amet, consectetur adipiscing elit. How functional Lorem ipsum dolor sit amet, consectetur adipiscing elit. Blog title heading will go here When the locking mechanism is activated UP/DOWN, LEFT/RIGHT articulating joint is fixed and cannot move, But OPEN/CLOSE action of End-Tool is possible. Your browser does not support the video tag. 360° End-Effector Rotation The NovaLap 360 D8 is designed to move beyond the natural limitations of the human hand. Its unique 360° end-effector rotation provides surgeons with full circular motion inside the body, enabling greater precision and access to challenging angles. This innovation delivers a natural “feel like hands” experience, enhancing control during delicate procedures. As a result, surgeons can perform seamless suturing, dissection, and knot tying—even in the tightest anatomical spaces. 8 Degrees of Freedom (D8) NovaLap 360 D8 offers eight degrees of freedom for advanced articulation. At its core, the end-effector consists of two individual parts that can function independently, giving surgeons precise and controlled movement without relying on interdependent mechanisms. This design ensures smooth, intuitive control for complex surgical maneuvers, while maintaining simplicity and ease of use. Your browser does not support the video tag. Ergonomic Handle with Finger Support The handle of NovaLap 360 D8 is designed to enhance comfort and reduce fatigue during prolonged surgeries. It features finger rests for steady handling and customizable silicone sleeves in varied sizes, allowing surgeons to choose the best fit for their hand. This adaptability ensures both stability and confidence while maintaining a lightweight, ergonomic design. Optimal Instrument Lengths To accommodate diverse surgical needs, NovaLap 360 D8 is available in multiple instrument lengths (25 cm, 38 cm, and 48 cm). Each length is engineered for compatibility with standard laparoscopic ports, providing surgeons with the ideal reach and accessibility for different procedures. This flexibility supports both comfort in handling and seamless integration into existing workflows. Product Name Lorem ipsum dolor sit amet, consectetur adipiscing elit. Product Name Lorem ipsum dolor sit amet, consectetur adipiscing elit. Complete lineup Lorem ipsum dolor sit amet, consectetur adipiscing elit. ( {p.title} {p.description} 0 ? ( Item Code Description Jaw Length Shaft Diameter Shaft Length Lock Electrode Units/Box Uses ( {spec.itemCode} {spec.productDescription} {spec.jawLength || \"-\"} {spec.shaftDiameter || \"-\"} {spec.shaftLength || \"-\"} {spec.lock} {spec.electrode} {spec.unitsBox || \"-\"} {spec.uses} ))} ) : ( No specifications available. )} ))} FAQs Lorem ipsum dolor sit amet, consectetur adipiscing elit. Suspendisse varius enim in eros elementum tristique. ( ))} Still have a question? Lorem ipsum dolor sit amet, consectetur adipiscing elit. Contact react ./css/productpage.css ../Layouts/Layouts ../assets/products/card-img.png ../assets/products/function2-img.png ../assets/products/functional-0.png ../assets/products/functional-1.png ../assets/products/functional-2.png ../assets/products/functional-3.png ../assets/products/360.mp4 ../components/AccordionItem react-router-dom react-router-dom framer-motion ../assets/products/d8.mp4 What is Lorem Ipsum? Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry , }, { id: 2, header: , text: Content here, content here , }, { id: 3, header: , text: , }, { id: 4, header: , text: t look even slightly believable. Product Name A /products/faqimg.png Short description for Product A. Replace with real copy. AUF01-F Fenestrated Forceps-38cm , shaftDiameter: , shaftLength: , lock: , electrode: , unitsBox: , uses: , }, { itemCode: , productDescription: , jawLength: , shaftDiameter: , shaftLength: , lock: , electrode: , unitsBox: , uses: , }, { itemCode: , productDescription: , jawLength: , shaftDiameter: , shaftLength: , lock: , electrode: , unitsBox: , uses: , }, ], }, { id: 2, title: , img: , description: , specs: [ { itemCode: , productDescription: , jawLength: , shaftDiameter: , shaftLength: , lock: , electrode: , unitsBox: , uses: , }, ], }, { id: 3, title: , img: , description: , specs: [], }, { id: 4, title: , img: , description: , specs: [], }, ]; const ProductPage = () => { const [activeProducts, setActiveProducts] = useState(null); const [activeFaq, setActiveFaq] = useState(null); const productActionRef = useRef(null); const location = useLocation(); useEffect(() => { if (location.hash) { const element = document.getElementById(location.hash.slice(1)); if (element) { element.scrollIntoView({ behavior: }); } } }, [location]); const handleToggleProducts = (id) => { setActiveProducts(activeProducts === id ? null : id); }; const handleToggleFaq = (id) => { setActiveFaq(activeFaq === id ? null : id); }; const handleInvertOn = () => { productActionRef.current?.classList.add( ); }; const handleInvertOff = () => { productActionRef.current?.classList.remove( ); }; const handleHoverOpenProducts = (id) => { setActiveProducts(id); }; const handleHoverCloseProducts = () => { setActiveProducts(null); }; return (        Describe what your company does in a few words    Describe exactly what the company does and what a customer can expect when working with the company. Avoid using verbose words or phrases.                 NovaLap 360 D8    NovaLap 360D8 is a multi-DoF articulating laparoscopic system that delivers robotic-like dexterity without robotic infrastructure.        Complete lineup                Your browser does not support the video tag.            How functional   Lorem ipsum dolor sit amet, consectetur adipiscing elit.             Blog title heading will go here    Lorem ipsum dolor sit amet, consectetur adipiscing elit. Suspendisse varius enim in eros. Lorem ipsum dolor sit amet, consectetur adipiscing elit.              Blog title heading will go here    Lorem ipsum dolor sit amet, consectetur adipiscing elit. Suspendisse varius enim in eros. Lorem ipsum dolor sit amet, consectetur adipiscing elit.                How functional   Lorem ipsum dolor sit amet, consectetur adipiscing elit.           Blog title heading will go here    When the locking mechanism is activated UP/DOWN, LEFT/RIGHT articulating joint is fixed and cannot move, But OPEN/CLOSE action of End-Tool is possible.                 Your browser does not support the video tag.        360° End-Effector Rotation    The NovaLap 360 D8 is designed to move beyond the natural limitations of the human hand. Its unique 360° end-effector rotation provides surgeons with full circular motion inside the body, enabling greater precision and access to challenging angles. This innovation delivers a natural “feel like hands” experience, enhancing control during delicate procedures. As a result, surgeons can perform seamless suturing, dissection, and knot tying—even in the tightest anatomical spaces.            8 Degrees of Freedom (D8)    NovaLap 360 D8 offers eight degrees of freedom for advanced articulation. At its core, the end-effector consists of two individual parts that can function independently, giving surgeons precise and controlled movement without relying on interdependent mechanisms. This design ensures smooth, intuitive control for complex surgical maneuvers, while maintaining simplicity and ease of use.         Your browser does not support the video tag.                Ergonomic Handle with Finger Support    The handle of NovaLap 360 D8 is designed to enhance comfort and reduce fatigue during prolonged surgeries. It features finger rests for steady handling and customizable silicone sleeves in varied sizes, allowing surgeons to choose the best fit for their hand. This adaptability ensures both stability and confidence while maintaining a lightweight, ergonomic design.            Optimal Instrument Lengths    To accommodate diverse surgical needs, NovaLap 360 D8 is available in multiple instrument lengths (25 cm, 38 cm, and 48 cm). Each length is engineered for compatibility with standard laparoscopic ports, providing surgeons with the ideal reach and accessibility for different procedures. This flexibility supports both comfort in handling and seamless integration into existing workflows.                  Product Name   Lorem ipsum dolor sit amet, consectetur adipiscing elit.             Product Name   Lorem ipsum dolor sit amet, consectetur adipiscing elit.             Complete lineup   Lorem ipsum dolor sit amet, consectetur adipiscing elit.      {faqProducts.map((p) => (   handleHoverOpenProducts(p.id)} onMouseLeave={handleHoverCloseProducts} >        {p.title}   {p.description}      {p.specs.length > 0 ? (        Item Code   Description   Jaw Length   Shaft Diameter   Shaft Length   Lock   Electrode   Units/Box   Uses        {p.specs.map((spec, i) => (    {spec.itemCode}   {spec.productDescription}   {spec.jawLength || }   {spec.shaftDiameter || }   {spec.shaftLength || }   {spec.lock}   {spec.electrode}   {spec.unitsBox || }   {spec.uses}    ))}     ) : (  No specifications available.  )}     ), }} />   ))}          FAQs    Lorem ipsum dolor sit amet, consectetur adipiscing elit. Suspendisse varius enim in eros elementum tristique.       {faqs.map((faq) => (   setActiveFaq(faq.id)} onMouseLeave={() => setActiveFaq(null)} >     ))}      Still have a question?   Lorem ipsum dolor sit amet, consectetur adipiscing elit.     "
  },
  {
    "title": "SearchResults",
    "path": "/SearchResults.jsx",
    "text": "(u && /^https?:\\/\\//i.test(u) ? u : `${apiOrigin}${u || \"\"}`); function stripHtml(input = \"\") { try { return String(input).replace(/ ({ type: \"broadcast\", id: b.id, title: b.title, excerpt: b.summary || stripHtml(b.body_html || \"\").slice(0, 300), date: (b.scheduled_at || b.created_at || \"\").slice(0, 10), image: abs(b.image_url || \"\"), link: b.link, raw: b, })); return [...pagesAsResults, ...broadcastAsResults]; }, [filteredBroadcasts, filteredPages]); const total = combined.length; const pages = Math.max(1, Math.ceil(total / perPage)); const shown = combined.slice((page - 1) * perPage, page * perPage); function onSubmitSearch(e) { e.preventDefault(); const q = (query || \"\").trim(); navigate(`/search?q=${encodeURIComponent(q)}`, { replace: true }); // update local state to trigger search effect setQuery(q); } function openResult(r) { if (r.type === \"broadcast\") { navigate(\"/news\", { state: { openId: r.id } }); } else if (r.type === \"page\" && r.path) { navigate(r.path); } } return ( Search {query ? {total.toLocaleString()} results : \"Type to start searching\"} Categories ▾ Dates ▾ Types ▾ {loading && Searching… } {error && {error} } {!loading && shown.length === 0 && query && No results found. ( {r.type === \"broadcast\" ? ( Broadcast {r.title} {r.date} = 300 ? \"…\" : \"\"} Read more → {r.link ? ( External ) : null} ) : ( 📄 Page {r.title} {r.path} {r.excerpt} Open page → )} ))} « First ‹ Prev Page {page} of {pages} Next › Last » react ../Layouts/Layouts react-router-dom ./css/searchresults.css ${window.location.protocol}//${window.location.host}/api http ${apiOrigin}${u || ); function stripHtml(input = ) { try { return String(input).replace(/ ]+(>|$)/g, ); } catch { return String(input || ); } } const menuItems = [ { title: , path: }, { title: , path: }, { title: , path: }, { title: , path: }, { title: , path: }, { title: , path: }, { title: , path: }, { title: , path: }, { title: , path: }, { title: , path: }, { title: , path: }, ]; export default function SearchResults() { const location = useLocation(); const navigate = useNavigate(); const params = new URLSearchParams(location.search); const qParam = params.get( ) || ; const [query, setQuery] = useState(qParam); const [loading, setLoading] = useState(false); const [error, setError] = useState( ); const [broadcasts, setBroadcasts] = useState([]); const [pageIndex, setPageIndex] = useState([]); const [filteredBroadcasts, setFilteredBroadcasts] = useState([]); const [filteredPages, setFilteredPages] = useState([]); // pagination const [page, setPage] = useState(1); const perPage = 8; useEffect(() => { // load static page index (async () => { try { const res = await fetch( , { cache: }); if (res.ok) { const data = await res.json(); if (Array.isArray(data)) setPageIndex(data); } } catch (e) { // ignore; page index optional console.warn( , e); } })(); }, []); useEffect(() => { if (!query || !query.trim()) { setFilteredBroadcasts([]); setFilteredPages([]); return; } const q = query.trim().toLowerCase(); let active = true; (async () => { setLoading(true); setError( ); try { const res = await fetch( , { cache: }); const ctype = res.headers.get( ) || ; if (!ctype.includes( )) { setError( ); setBroadcasts([]); setFilteredBroadcasts([]); setLoading(false); return; } const data = await res.json(); const items = Array.isArray(data.items) ? data.items : data.items ? [data.items] : []; if (!active) return; setBroadcasts(items); } catch (err) { console.error( , err); setError( ); setBroadcasts([]); } finally { if (active) setLoading(false); } })(); return () => { active = false; }; }, [query]); useEffect(() => { if (!query || !query.trim()) { setFilteredBroadcasts([]); setFilteredPages([]); return; } const needle = query.trim().toLowerCase(); const matchedB = (broadcasts || []).filter((b) => { const title = (b.title || ).toLowerCase(); const summary = (b.summary || ).toLowerCase(); const body = stripHtml(b.body_html || ).toLowerCase(); return title.includes(needle) || summary.includes(needle) || body.includes(needle) || (b.tag || ).toLowerCase().includes(needle); }); setFilteredBroadcasts(matchedB); const matchedP = (pageIndex || []).filter((p) => { const title = (p.title || ).toLowerCase(); const path = (p.path || ).toLowerCase(); const text = (p.text || ).toLowerCase(); return title.includes(needle) || path.includes(needle) || text.includes(needle); }); setFilteredPages(matchedP); setPage(1); }, [query, broadcasts, pageIndex]); // combined results for count and pagination (pages first, then broadcasts) const combined = useMemo(() => { const pagesAsResults = (filteredPages || []).map((p) => ({ type: , id: p.path || p.title, title: p.title, path: p.path, excerpt: (p.text || ).slice(0, 300), })); const broadcastAsResults = (filteredBroadcasts || []).map((b) => ({ type: , id: b.id, title: b.title, excerpt: b.summary || stripHtml(b.body_html || ).slice(0, 300), date: (b.scheduled_at || b.created_at || ).slice(0, 10), image: abs(b.image_url || ), link: b.link, raw: b, })); return [...pagesAsResults, ...broadcastAsResults]; }, [filteredBroadcasts, filteredPages]); const total = combined.length; const pages = Math.max(1, Math.ceil(total / perPage)); const shown = combined.slice((page - 1) * perPage, page * perPage); function onSubmitSearch(e) { e.preventDefault(); const q = (query || ).trim(); navigate( , { replace: true }); // update local state to trigger search effect setQuery(q); } function openResult(r) { if (r.type === ) { navigate( , { state: { openId: r.id } }); } else if (r.type === && r.path) { navigate(r.path); } } return (           setQuery(e.target.value)} aria-label= />  Search        {query ? <> {total.toLocaleString()}  results  : }      Categories ▾   Dates ▾   Types ▾        {loading &&  Searching… } {error &&  {error} } {!loading && shown.length === 0 && query &&  No results found. } {shown.map((r) => (   openResult(r)} role= tabIndex={0}> {r.type === ? ( <>          Broadcast   {r.title}   {r.date}   {r.excerpt}{r.excerpt && r.excerpt.length >= 300 ? : }      { e.stopPropagation(); openResult(r); }}>Read more →  {r.link ? ( e.stopPropagation()}>External ) : null}       ) : ( <>    📄       Page   {r.title}   {r.path}   {r.excerpt}      { e.stopPropagation(); openResult(r); }}>Open page →        )}   ))}         setPage(1)} className= >« First    setPage((p) => Math.max(1, p - 1))} className= >‹ Prev    Page  {page}  of {pages}     setPage((p) => Math.min(pages, p + 1))} className= >Next ›    setPage(pages)} className="
  },
  {
    "title": "SocialPostAdmin",
    "path": "/SocialPostAdmin.jsx",
    "text": "(image ? URL.createObjectURL(image) : ''), [image]); async function handleSubmit(e) { e.preventDefault(); setMsg(''); if (!title || !description || !image) { setMsg('Please fill title, description and select an image.'); return; } const fd = new FormData(); fd.append('title', title); fd.append('description', description); fd.append('platform', platform); if (postUrl) fd.append('post_url', postUrl); fd.append('image', image); try { setSubmitting(true); const res = await fetch(`${API_BASE}/create_post.php`, { method: 'POST', body: fd, }); const data = await res.json(); if (!data.ok) throw new Error(data.error || 'UPLOAD_FAILED'); setMsg('✅ Post saved successfully!'); setTitle(''); setDescription(''); setPlatform('linkedin'); setPostUrl(''); setImage(null); } catch (err) { setMsg('❌ ' + err.message); } finally { setSubmitting(false); } } return ( Add Social Post Enter your LinkedIn (or other) post details. This will save to your MySQL database and appear on your website automatically. Title Description Platform LinkedIn X (Twitter) Instagram YouTube Other Post URL (optional) Image {preview && ( )} {submitting ? 'Saving…' : 'Save Post'} {msg && {msg} } {` .label{display:block;font-weight:600;margin:8px 0 6px} .input, .textarea, select{width:100%;padding:10px 12px;border:1px solid #ddd;border-radius:8px} .button{padding:10px 14px;border:none;border-radius:10px;background:#111;color:#fff;cursor:pointer} .button:disabled{opacity:.6;cursor:not-allowed} `} react react-router-dom framer-motion ../Layouts/Layouts ./css/aboutpage.css https://localhost/api ); const [description, setDescription] = useState( ); const [platform, setPlatform] = useState( ); const [postUrl, setPostUrl] = useState( ); const [image, setImage] = useState(null); const [submitting, setSubmitting] = useState(false); const [msg, setMsg] = useState( ); const preview = useMemo(() => (image ? URL.createObjectURL(image) : ), [image]); async function handleSubmit(e) { e.preventDefault(); setMsg( ); if (!title || !description || !image) { setMsg( ); return; } const fd = new FormData(); fd.append( , title); fd.append( , description); fd.append( , platform); if (postUrl) fd.append( , postUrl); fd.append( , image); try { setSubmitting(true); const res = await fetch( , { method: , body: fd, }); const data = await res.json(); if (!data.ok) throw new Error(data.error || ); setMsg( ); setTitle( ); setDescription( ); setPlatform( ); setPostUrl( ); setImage(null); } catch (err) { setMsg( + err.message); } finally { setSubmitting(false); } } return (         Add Social Post    Enter your LinkedIn (or other) post details. This will save to your MySQL database and appear on your website automatically.      Title   setTitle(e.target.value)} placeholder= />  Description   setDescription(e.target.value)} placeholder= />      Platform   setPlatform(e.target.value)}>  LinkedIn   X (Twitter)   Instagram   YouTube   Other         Post URL (optional)   setPostUrl(e.target.value)} placeholder= />      Image   setImage(e.target.files?.[0] || null)} /> {preview && (   )}   {submitting ? : }     {msg &&  {msg} }  {"
  },
  {
    "title": "Timeline",
    "path": "/Timeline.jsx",
    "text": "{ const { scrollYProgress } = useScroll(); // Get scroll progress (0 to 1) // Transform X position based on scroll progress const x = useTransform(scrollYProgress, [0, 1], [\"0%\", \"100%\"]); return ( framer-motion 100% 200vh 50px 100px 100px blue fixed 50%"
  },
  {
    "title": "VideoPage",
    "path": "/VideoPage.jsx",
    "text": "setMobileView(window.innerWidth t.key === topCat)?.label || \"\"; return ( Videos Browse {topLabel} videos by sub-category. {/* Top categories */} {mobileView ? ( ( {c.label} ))} ( {c.label} )) )} {/* Sub-categories (classification) */} {topLabel} Videos {/* Filter by sub-category. */} {mobileView ? ( {subcats.length ? null : All ( {sc.name} ))} ) : ( {subcats.length === 0 ? ( All ( {sc.name} ))} )} {/* Player + carousel */} {loading ? ( Loading… ) : embedUrl ? ( ) : fileUrl ? ( ) : ( No video selected. )} {videoTitle} react ./css/videopage.css ../Layouts/Layouts ../components/VideoPlayer react-router-dom ../components/SwiperCarousel framer-motion ../utils/youtube /api product Product surgery Surgery training Training ; const m = String(url).match( /(?:https?:\\/\\/)?(?:www\\.)?(?:youtube\\.com\\/watch\\?v=|youtu\\.be\\/)([\\w-]{11})/ ); return m ? : ; } export default function VideoPage() { const [topCat, setTopCat] = useState( ); const [subcats, setSubcats] = useState([]); // [{id,name}] const [subcatId, setSubcatId] = useState( ); // | number const [videos, setVideos] = useState([]); // API rows const [loading, setLoading] = useState(false); // UI bits you already had const [videoTitle, setVideoTitle] = useState( ); const [embedUrl, setEmbedUrl] = useState( ); // iframe, when youtube const [fileUrl, setFileUrl] = useState( ); // non-youtube video src const [mobileView, setMobileView] = useState(false); // location hash scroll (unchanged) const location = useLocation(); useEffect(() => { if (location.hash) { const el = document.getElementById(location.hash.slice(1)); if (el) el.scrollIntoView({ behavior: }); } }, [location, subcatId]); // responsive helper (unchanged) useEffect(() => { const onResize = () => setMobileView(window.innerWidth   window.removeEventListener( , onResize); }, []); // load sub-categories whenever topCat changes useEffect(() => { (async () => { try { const res = await fetch( ); const data = await res.json(); const items = Array.isArray(data?.items) ? data.items : []; setSubcats(items); setSubcatId(items[0]?.id ? String(items[0].id) : ); } catch { setSubcats([]); setSubcatId( ); } })(); }, [topCat]); // load videos when topCat or subcatId changes useEffect(() => { (async () => { setLoading(true); try { const u = new URL( , window.location.origin); u.searchParams.set( , topCat); if (subcatId !== ) u.searchParams.set( , subcatId); const res = await fetch(u.toString()); const data = await res.json(); const items = Array.isArray(data?.items) ? data.items : []; setVideos(items); // pick “hero” video const first = items[0]; if (first) { setVideoTitle(first.title || ); const yt = toYouTubeEmbed(first.youtube_url); setEmbedUrl(yt); setFileUrl(yt ? : (first.src_url || )); } else { setVideoTitle( ); setEmbedUrl( ); setFileUrl( ); } } finally { setLoading(false); } })(); }, [topCat, subcatId]); // adapt API results to SwiperCarousel’s expected shape const classificationName = useMemo(() => { if (subcatId === ) return ; return subcats.find((s) => String(s.id) === String(subcatId))?.name || ; }, [subcatId, subcats]); const videoCardDetials = useMemo(() => { const mapped = videos.map(v => { const id = getYouTubeId(v.youtube_url); const auto = id ? ytThumb(id, ) : ; return { id: v.id, // order: explicit thumbnail from DB → YouTube auto → local placeholder link: v.thumbnail_url || auto || , name: v.title, play: v.youtube_url || v.src_url || , }; }); return { [topCat]: { [classificationName]: mapped, }, }; }, [videos, topCat, classificationName]); const topLabel = TOPS.find(t => t.key === topCat)?.label || ; return (           Videos   Browse {topLabel} videos by sub-category.        {/* Top categories */}     {mobileView ? (   setTopCat(e.target.value)} > {TOPS.map((c) => (  {c.label}  ))}   ) : ( TOPS.map((c) => (   setTopCat(c.key)} > {c.label}   )) )}     {/* Sub-categories (classification) */}    {topLabel} Videos  {/*  Filter by sub-category.  */}   {mobileView ? (   setSubcatId(e.target.value)} > {subcats.length ? null :  All } {subcats.map((sc) => (  {sc.name}  ))}   ) : ( <> {subcats.length === 0 ? (  All  ) : null} {subcats.map((sc, idx) => (   { setSubcatId(String(sc.id)); const el = document.getElementById( ); if (el) window.scrollTo({ top: el.offsetTop, behavior: }); }} > {sc.name}   ))}   )}     {/* Player + carousel */}     {loading ? (  Loading…  ) : embedUrl ? (   ) : fileUrl ? (   ) : (  No video selected.  )}      {videoTitle}      { const yt = toYouTubeEmbed(url); setEmbedUrl(yt); setFileUrl(yt ?"
  }
]